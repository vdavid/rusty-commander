---
description: How to add an npm/pnpm dependency to the project
---

1. Run `pnpm add <package>` (or `pnpm add -D <package>` for dev deps)
2. Run `./scripts/check.sh --svelte` to ensure everything passes
3. Do not commit unless asked to
